# Default values for web-app
# This is a YAML-formatted file
# Declare variables to be passed into your templates.

replicaCount: 2
image:
  repository: yalecthub/web-app
  pullPolicy: IfNotPresent
  tag: 1
